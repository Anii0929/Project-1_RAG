# Copy this file to .env and configure your AI provider

# Choose AI Provider (ollama, huggingface, openai_compatible, search_only, anthropic)
AI_PROVIDER=ollama

# Anthropic settings (only needed if AI_PROVIDER=anthropic)
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Ollama settings (only needed if AI_PROVIDER=ollama)
OLLAMA_MODEL=llama2

# OpenAI Compatible settings (only needed if AI_PROVIDER=openai_compatible)
OPENAI_COMPATIBLE_URL=http://localhost:5000/v1
OPENAI_COMPATIBLE_MODEL=local-model